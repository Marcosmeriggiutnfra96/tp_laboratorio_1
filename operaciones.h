#include <stdlib.h>
#include <stdio.h>
/** \brief RECIBE LOS VALORES Y DEVUELVE EL RESULTADO
 * \return LA SUMA
 *
 */
float suma(float num1,float num2);

/** \brief RECIBE LOS VALORES Y DEVUELVE EL RESULTADO
 *
 * \param REALIZA LA OPERACION RESTA
 * \return EL RESULTADO
 *
 */
float resta(float num1,float num2) ;

/** \brief RECIBE LOS VALORES Y DEVUELVE EL RESULTADO
 *
 * \param  REALIZA LA OPERACION MULTIPLICACION
 * \return EL RESULTADO
 *
 */
float multiplicacion(float num1,float num2) ;

/** \brief  RECIBE LOS VALORES Y DEVUELVE EL RESULTADO
 *
 * \param realiza la operacion divicion
 * \return el resultado
 *
 */

float dividir(float num1, float num2);

/** \brief recive un valor y retorna el resultado
 *
 * \param comprueba que el valor ingresado sea mayor a 0
 * \param realiza el factorial
 * \return el resultado
 *
 */

int factorial(int num);
